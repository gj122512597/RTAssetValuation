# 2026-07-31

## 当前分支 rtdemo：为客户部署做功能删减
- 按需求删除**全部尽调（Due Diligence）工作台**页面/功能/数据，仅作用于 `rtdemo` 工作区（未提交）。
- 删除/修改清单：
  - 删除页面：`src/pages/DueDiligenceCenter.tsx`、`DueDiligenceNewPage.tsx`、`DueDiligenceIntakePage.tsx`
  - 删除组件：`src/components/due_diligence/DueDiligenceCard.tsx`、`DueDiligenceOverview.tsx`（后者原被 AssetDetailPage 引用，已移除该 section）
  - 删除数据：`src/mocks/intake_assets.json`、`src/mocks/due_diligence_templates.ts`、`dist/assets/intake_assets-BBBp0nis.json`
  - 改 `src/App.tsx`：移除 3 条 /due-diligence 路由与导入
  - 改 `src/types/index.ts`：移除 CheckResult/DueDiligenceCheck/DueDiligenceTemplate/DueDiligenceProgress/IntakeSource/IntakeStatus/IntakeAsset
  - 改 `src/stores/assetStore.ts`：移除 dueDiligence/intakeAssets 状态、initDueDiligence/setCheckResult/resetDueDiligence/setIntakeStatus/syncIntakeToDueDiligence 及 intake_assets 拉取
  - 改 `src/components/dashboard/ProcessFlowBanner.tsx`：流程条由 4 阶段降为 3 阶段（移除「尽调工作台」）
  - 同步更新 `README.md`、`system-intro.html`（移除 M7 / 尽调阶段 / P5 段落）
- `npm run lint`（tsc --noEmit）通过，无类型错误。
- **main 分支未改动**，仍含全部尽调文件，可随时切回恢复。
- 待确认：`ppt/` 客户汇报 deck（独立演示文稿，非部署的 React 应用）仍含 ~35 处「尽调」引用，是否一并清理待用户决定。

## 构建 arm64 部署镜像（客户环境）
- 构建产物：`rt-asset-valuation-web:latest` 与 `rt-asset-valuation-api:latest`（均 `linux/arm64`，本机 Apple Silicon 原生构建）。
- 交付 tar：`/Users/johnson/CodeBuddy/RTAssetValuation/rt-asset-arm64-images.tar`（含两镜像，`docker load -i` 还原）。
- **Docker 镜像源坑**：本机 `~/.docker/daemon.json` 含失效镜像源 `docker.m.daocloud.io`（拉取 401）；且 Docker Desktop 每次重启会把它还原。中科大/百度源在本环境 EOF 不可达。绕过法：`docker pull --platform linux/arm64 node:20-alpine` 与 `nginx:1.27-alpine` 可直连 docker.io 回退成功，基础镜像进本地缓存后 `docker buildx build` 直接用缓存，不再走坏源。
- 关键架构事实：**前端 web 镜像自包含**——核心 225 资产/325 竞品/876 成交等数据构建时打包进镜像（store 直接 fetch 静态 mock），故 web 单独跑即可完整展示估价系统；后端 api 仅用于情报站/爬虫持久化，属可选，且 `db:seed` 只灌样例数据（非全量，全量在前端 mock）。
- AMap Key 已用 `.env` 的 VITE_AMAP_KEY/VITE_AMAP_SECURITY 经 build-arg 编入 web 镜像；客户部署需把该 Key 的 Referer/域名白名单加入客户域名，否则地图被拦。换 Key 需重新 `docker buildx build --build-arg VITE_AMAP_KEY=... --build-arg VITE_AMAP_SECURITY=...`。
- 前端 API 地址 `VITE_API_BASE_URL` 构建期内联（`src/api/client.ts`），默认 `http://localhost:3001/api`，跨机访问后端会失效。已在根 `Dockerfile` 补 `ARG/ENV VITE_API_BASE_URL`，全栈部署时须 `--build-arg VITE_API_BASE_URL=http://<客户IP>:3001/api` 重建 web。
- 已产出交付文档 `DEPLOY.md`（根目录）：交付物清单、docker load、3a 单前端 / 3b 全栈 compose、arm64 限制、AMap 白名单、API 地址固化、重建命令、镜像源坑、运维备份命令。

## 修复：api 镜像在客户空数据卷首启崩溃（`no such table: hedonic_models`）
根因两处（本地开发因 db 文件已存在而长期未暴露）：
1. `server/src/routes/models.ts` 顶层直接 `ensureSeed()` —— ESM import 求值早于 `index.ts` 主体的 `initSchema()`，空库时查 `hedonic_models` 必崩。改为 `export function ensureSeed()`，由 `index.ts` 在 `initSchema()` **之后**调用（顺序：initSchema → ensureModelSeed → initTrainingSamples）。
2. `server/src/db.ts` 的 `initSchema()` 读 `join(__dirname,'schema.sql')`，但容器内 `__dirname=/app/dist` 而 Dockerfile 只把 schema.sql 拷到 `/app/src/` → 修完 bug1 后会接着 ENOENT。已改为 candidates 回退查找（`./schema.sql` → `../src/schema.sql` → `../schema.sql`），并在 `server/Dockerfile` 额外拷一份到 `./dist/schema.sql`（tsc 不搬运 .sql）。
已重建 api 镜像并用全新空卷验证通过（healthy，/health 200，/api/models/hedonic/comparative 返回模型），`rt-asset-arm64-images.tar` 已重新导出。

## 前端移动端响应式适配（全站，PC 布局不变）
用户反馈手机浏览器访问页面布局乱、互相遮挡。方案：新增 `src/hooks/useIsMobile.ts`（基于 antd `Grid.useBreakpoint()`，`!screens.md` 即 <768px 判为移动端），**PC 端布局完全保留，仅 ≤768px 切换移动布局**。
- `HomePage`：顶栏由 `right-[336px]` 改为移动端全宽；右侧 320px 浮层（`overviewPanel`）在移动端改为右下 FAB（`UnorderedListOutlined`）唤起的 antd `Drawer`（`width="88%"`）。
- `AssetDetailPage`：`grid-cols-5` 多列 → 移动端 `grid-cols-1` 单列堆叠；地图 `h-[55vh]`→`h-[40vh]`；报告 `Drawer width={720}`→`100%`；头部 `flex-wrap`。
- `ProcessFlowBanner`/`StatBar`：移动端 `flex-wrap`、隐藏阶段描述与分隔线，避免横向溢出。
- `NewAssetValuationPage`：头部 `flexWrap`、外容器 padding 响应式（其 `Row/Col` 本就 `xs={24}` 已单列）。
- `IntelPage`/`ModelingIntroPage`：头部 `flex-wrap` + 内边距改 `md:` 断点（无需 JS 钩子）。
- `tsc --noEmit` 与 `npm run build` 均通过。注：并行 replace 时工具常误报「not found」但实际已改，以搜索确认终态为准；导入语句锚点需与实际文本完全一致。

## 重新构建交付镜像（含移动端适配 + 客户 API 地址）
- 重新构建 `rt-asset-valuation-web:latest`（arm64）：注入 AMap Key（`025e32f6796944d7782bfe64fe3c288b`）+ Security（`124741ecb35523cc311e368eeceacb3f`，取自 `.env`），并 `--build-arg VITE_API_BASE_URL=http://218.203.49.164:3001/api`（移动端适配已在源码中，无需改）。
- 构建命令用普通 `docker build --platform linux/arm64 -t ...`（本机 Apple Silicon 原生）；`docker buildx build --load` 曾被 watch 超时截断、未真正更新镜像（ID 不变），改普通 build 后成功（web 新 ID `019f00986e43`，api 新 ID `a23dd3f814e3`）。
- 校验：容器内能 grep 到 bundle 内 `218.203.49.164:3001`，且 `localhost:3001/api` 兜底被 minifier 常量折叠消除；新 bundle 文件名 `index-BaxbRpyD.js`。
- 同时用当前 `server/` 源码重建 `rt-asset-valuation-api:latest`（含近期未提交修复）。
- 重新打包 `rt-asset-arm64-images.tar`（= docker save web+api）；新 docker 版为 OCI 布局（blobs/index.json），体积 81M（gzip 压缩+去重，含两镜像，属正常）。已用 `index.json` 确认两 manifest 均在内。

## 修复：Hedonic 训练页「重训模型」报 500 spawn python3 ENOENT
根因（api 容器 `node:20-alpine` 缺失 Python 相关全部链路）：
1. 镜像无 python3 → `spawn python3 ENOENT`（直接报错）。
2. `fit_hedonic.py` 未被 COPY 进 runtime 镜像（route 解析路径 `/app/dist/scripts/fit_hedonic.py`），文件缺失。
3. 脚本 `import numpy`，alpine 默认 python3 无 numpy。
4. 脚本原硬编码 `_ROOT = 上溯 4 层` 求 DB 路径，仅匹配本地 `server/src/scripts` 布局；容器内放 `/app/dist/scripts` 会上溯到 `/`，错位。
修复：
- `server/Dockerfile` runtime 阶段 `RUN apk add --no-cache python3 py3-numpy`；并 `COPY --from=builder /app/src/scripts/fit_hedonic.py ./dist/scripts/fit_hedonic.py`。
- `server/src/scripts/fit_hedonic.py`：`DB_PATH = os.environ.get("DB_PATH") or os.path.join(_ROOT,"data","rt_asset.db")`，优先用与后端一致的 `DB_PATH` 环境变量（容器内 `/app/data/rt_asset.db`）。
验证：容器内 `python3 3.12` + `numpy 2.3.5` 可用、脚本就位；启动容器触发 seed 后 `POST /api/training-samples/refit` 返回 `{"ok":true}` 并打印真实系数/R²（comparative/historical 两模型均已写库）。
已重建 api 镜像（`d7cbfe41d881`）并重新 `docker save` 进 `rt-asset-arm64-images.tar`（现 109M，含 python 层）。

## 修复：手机端顶部「业务主流程」看似消失
现象：移动端浏览时，顶部 ProcessFlowBanner 的 3 阶段流水线看不到。
根因：`ProcessFlowBanner.tsx` 外层 banner 与「业务主流程」容器在移动端已 `flex-wrap`，但内部 3 阶段流水线 div（`flex min-w-0 flex-1 items-center gap-1.5`）缺 `flex-wrap`，其 3 个 `shrink-0` 阶段(~280px)超出可用宽度，溢出到屏幕右缘被裁切，故看似「没了」。
修复：给该流水线 div 加 `flex-wrap md:flex-nowrap`（移动端换行显示）。`tsc` 与 `build` 通过。
已重建 web 镜像（`f92974d251ef`，新 bundle `index-CLfnXjt7.js`）并重新 `docker save` 进 `rt-asset-arm64-images.tar`（109M）。
注：`docker build` 命令若带 `| tail` 或实时输出会被工具误判为 watch 而 10s 截断（exitCode 仍报 0 但镜像未更新）；稳妥写法是 `> log 2>&1; echo EXIT=$?`（不接 tail）。
